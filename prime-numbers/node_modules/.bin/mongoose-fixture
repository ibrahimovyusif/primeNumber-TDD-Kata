#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongoose-fixture/bin/mongoose-fixture" "$@"
  ret=$?
else 
  node  "$basedir/../mongoose-fixture/bin/mongoose-fixture" "$@"
  ret=$?
fi
exit $ret
